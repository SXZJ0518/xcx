# 开发环境配置
NODE_ENV=development

# API 基础地址（开发阶段用 Mock 数据，不需要真实地址）
VUE_APP_API_BASE_URL=http://localhost:3000/api

# 是否使用 Mock 数据【部署到 Vercel 时改为 false = 走真实 API】
VUE_APP_USE_MOCK=true

# 云函数地址（上线时替换）
VUE_APP_CLOUD_FUNCTION_URL=
